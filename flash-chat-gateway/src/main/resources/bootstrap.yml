spring:
  application:
    name: flash-chat-gateway
  profiles:
    active: dev
  config:
    import:
      - nacos:flash-chat-gateway-dev.yaml
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml